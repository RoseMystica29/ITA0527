import cv2

image = cv2.imread("C:\Users\Johnhenry\OneDrive\Desktop\sample")

if image is None:
    print("Error: Image not found")
    exit()

image_small = cv2.resize(image, (500, 350))

rotated = cv2.rotate(image_small, cv2.ROTATE_90_CLOCKWISE)

cv2.imshow("Original Image", image_small)
cv2.imshow("Rotated 90 Degree CW", rotated)

cv2.waitKey(0)
cv2.destroyAllWindows()
