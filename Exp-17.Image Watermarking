import cv2

img = cv2.imread("C:/Users/Johnhenry/OneDrive/Desktop/sample")
watermark = img.copy()

cv2.putText(watermark, "ROSE", (50,100),
            cv2.FONT_HERSHEY_SIMPLEX,
            2, (0,0,255), 3)

cv2.imshow("Original", cv2.resize(img, (600,400)))
cv2.imshow("Watermarked Image", cv2.resize(watermark, (600,400)))

cv2.waitKey(0)
cv2.destroyAllWindows()
